import { InputErrorDirective } from './InputErrorDirective';

describe('InputErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new InputErrorDirective();
    expect(directive).toBeTruthy();
  });
});
